// app/user/write/layout.js

export const metadata = {
    title: "방명록 작성",
};

export const viewport = {
    width: "device-width",
    initialScale: 1,
};

export default function WriteLayout({ children }) {
    return <>{children}</>;
}
